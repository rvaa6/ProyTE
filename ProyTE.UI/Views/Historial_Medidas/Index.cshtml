@model IEnumerable<ProyTE.UI.Models.TbHistorial_Medidas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_HistorialMedidas)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Id_Usuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Medida)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_HistorialMedidas)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Id_Usuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Medida)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id = item.Id_HistorialMedidas }) |
            @Html.ActionLink("Detalles", "Details", new { id = item.Id_HistorialMedidas }) |
            @Html.ActionLink("Eliminar", "Delete",
                new { confirm = true, id = item.Id_HistorialMedidas },
                new { onclick = "return confirm('¿Esta seguro de eliminar el historial?')" })
        </td>
    </tr>
}

</table>
